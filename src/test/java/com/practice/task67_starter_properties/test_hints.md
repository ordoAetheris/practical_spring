# Подсказки: Starter Properties
## Обязательно
- audit.enabled=true → AuditLogger configured with enabled=true.
- Validation: audit.sink-type blank → startup failure.
- IDE: autocomplete for audit.* properties (metadata.json).
