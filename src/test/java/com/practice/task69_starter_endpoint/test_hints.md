# Подсказки: Starter Actuator Endpoint
## Обязательно
- GET /actuator/audit → JSON list of recent events.
- POST /actuator/audit → cleared.
- Endpoint exposed via management.endpoints.web.exposure.include.
